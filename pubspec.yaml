name: purple_lints
description: Purplesoft commonly used set of lints.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.4.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_lints:
  dart_code_metrics: 

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter: